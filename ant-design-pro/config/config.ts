import { defineConfig } from '@umijs/max';
import routes from './routes';

export default defineConfig({
  title: '杜同学',
  antd: {},
  access: {},
  model: {},
  initialState: {},
  request: {},
  layout: {},
  routes,
  npmClient: 'yarn',
});
